# 版本七：扩展架构版

## 版本目标

在单机基础 IM 能力完成后，规划生产化扩展能力，包括外部身份适配、多实例投递、在线状态、精细已读回执和消息搜索。

v7 是扩展能力集合，实际开发时可以继续拆成多个独立子版本。

## 前置基础

- v1 已实现本地用户和 JWT
- v4 已实现单进程 WebSocket 实时投递
- v5 已实现设备记录
- v6 已实现基础群管理和消息状态

## 数据库文档

本版本涉及数据库表：

- [数据库表创建-消息回执](../数据库/数据库表创建-消息回执.md)
- [数据库表关系](../数据库/数据库表关系.md)

本版本也会继续使用：

- [数据库表创建-人员](../数据库/数据库表创建-人员.md)
- [数据库表创建-消息](../数据库/数据库表创建-消息.md)

## 本版本新增能力

- 外部身份系统适配
- 多实例 WebSocket 投递
- 在线状态查询
- 逐条消息送达和已读回执
- 消息搜索

## 子版本拆分建议

v7 包含多个相对独立的扩展能力。实际开发时建议拆成以下子版本逐个落地：

- v7.1：外部身份适配
- v7.2：Redis 在线状态与多实例投递
- v7.3：精细消息回执
- v7.4：消息搜索

每个子版本都可以单独开发、测试和上线。

## 代码落地总清单

v7.1 外部身份适配新增：

- `models/auth_provider.go`：定义 `AuthProvider`、`TokenVerifier`、`UserSyncer` 接口
- `models/external_user_model.go`：根据 `external_id` 查询或同步用户
- `controllers/external_auth_controller.go`：处理外部登录态换取 IM token

v7.2 Redis 在线状态与多实例投递新增：

- `models/presence_model.go`：在线状态 DTO 和查询方法
- `models/realtime_bus.go`：定义跨实例投递接口
- `models/redis_realtime_bus.go`：Redis Pub/Sub 实现
- `controllers/presence_controller.go`：在线状态查询接口

v7.3 精细消息回执新增：

- `models/message_receipt_model.go`：消息回执模型和读写方法
- `controllers/message_receipt_controller.go`：消息回执接口

v7.4 消息搜索新增：

- `models/message_search_model.go`：消息搜索方法
- `controllers/message_search_controller.go`：消息搜索接口

需要修改：

- `models/user_model.go`：补充外部用户同步方法或复用 `external_id`
- `controllers/ws_controller.go`：投递成功后写入 delivered，接入跨实例 bus
- `models/ws_hub.go`：支持本机连接查询和跨实例事件处理
- `routers/router.go`：注册外部认证、在线状态、回执、搜索接口
- `conf/app.ini`：增加 Redis 或外部认证配置

## v7.1 外部身份适配落地

新增接口：

```text
POST /api/auth/external
```

请求体示例：

```json
{
  "provider": "demo",
  "access_token": "external-token"
}
```

响应沿用 v1 登录响应：

```json
{
  "code": 200,
  "data": {
    "user": {
      "id": 10,
      "external_id": "external-user-001",
      "username": "external-user-001",
      "nickname": "外部用户",
      "auth_source": "external",
      "status": 1
    },
    "token": "<jwt-token>"
  },
  "message": "登录成功"
}
```

需要实现的方法：

- `func FindUserByExternalID(externalID string) (User, error)`
- `func SyncExternalUser(profile ExternalUserProfile) (User, error)`
- `func LoginExternalUser(provider string, accessToken string) (User, error)`

落地顺序：

1. 定义 `ExternalUserProfile`。
2. 定义 `TokenVerifier` 接口。
3. 先实现一个 `DemoTokenVerifier`，用于本地开发验证。
4. 实现 `FindUserByExternalID`。
5. 实现 `SyncExternalUser`，不存在则创建，存在则更新昵称和头像。
6. 新增 `ExternalAuthController`。
7. 注册 `/api/auth/external`。
8. 验证同一个外部用户多次登录得到同一个内部用户 ID。

## v7.2 Redis 在线状态与多实例投递落地

建议新增配置：

```ini
[redis]
addr = 127.0.0.1:6379
password =
db = 0
```

建议新增接口：

```text
GET  /api/users/{user_id}/presence
POST /api/users/presence/batch
```

需要实现的接口抽象：

```go
type RealtimeBus interface {
	PublishMessageDeliver(event MessageDeliverEvent) error
	SubscribeMessageDeliver(handler func(MessageDeliverEvent)) error
}
```

Redis key 建议：

```text
presence:user:{user_id}
```

value 示例：

```json
{
  "user_id": 1,
  "instance_id": "server-001",
  "connection_count": 2,
  "last_seen_at": "2026-06-29T12:00:00+08:00"
}
```

落地顺序：

1. 增加 Redis 配置读取。
2. 增加 `RealtimeBus` 接口。
3. 实现 Redis Pub/Sub 发布和订阅。
4. WebSocket 建连时写入在线状态。
5. WebSocket 断开时更新连接数量和 `last_seen_at`。
6. 发送消息后发布跨实例投递事件。
7. 每个实例订阅投递事件，只投递本机持有的连接。
8. 增加在线状态查询接口。

## v7.3 精细消息回执落地

需要先执行 [数据库表创建-消息回执](../数据库/数据库表创建-消息回执.md)。

新增接口：

```text
GET  /api/messages/{message_id}/receipts
POST /api/messages/{message_id}/delivered
POST /api/messages/{message_id}/read
```

需要实现的 model 方法：

- `func UpsertMessageReceipt(messageID int64, userID int64, status string) error`
- `func MarkMessageDelivered(messageID int64, userID int64) error`
- `func MarkMessageRead(messageID int64, userID int64) error`
- `func ListMessageReceipts(requestUserID int64, messageID int64) ([]MessageReceiptDTO, error)`

落地顺序：

1. 新增 `models/message_receipt_model.go`。
2. 实现回执 upsert，`read` 可以覆盖 `delivered`。
3. WebSocket 投递成功后写入 `delivered`。
4. 客户端读到消息时调用 `read` 接口。
5. 查询回执前校验请求用户是会话成员。

## v7.4 消息搜索落地

初期不引入搜索引擎，先做 MySQL 能力范围内的文本消息搜索。

新增接口：

```text
GET /api/conversations/{conversation_id}/messages/search?q=hello&limit=20
GET /api/messages/search?q=hello&limit=20
```

需要实现的 model 方法：

- `func SearchConversationMessages(userID int64, conversationID int64, keyword string, limit int) ([]MessageDTO, error)`
- `func SearchMyMessages(userID int64, keyword string, limit int) ([]MessageDTO, error)`

规则：

- 只搜索 `message_type = text`
- 只返回 `status = normal`
- 会话内搜索必须校验当前用户是会话成员
- 全局搜索只查当前用户参与的会话
- `keyword` 去空格后不能为空
- `limit` 默认 `20`，最大 `100`

落地顺序：

1. 新增 `models/message_search_model.go`。
2. 实现会话内搜索。
3. 实现当前用户全局搜索。
4. 新增 `controllers/message_search_controller.go`。
5. 注册搜索路由。
6. 验证非成员不能搜索别人的会话消息。

## 外部身份适配

### 目标

允许客户端先在外部系统登录，再携带外部登录态访问 IM 服务。IM 服务通过外部身份映射到内部 `users.id`。

### 关键抽象

```text
AuthProvider
TokenVerifier
UserSyncer
```

职责：

- `AuthProvider`：处理本地登录或外部登录入口
- `TokenVerifier`：校验请求携带的 token 或外部登录态
- `UserSyncer`：根据外部用户资料创建或更新 `users`

### 数据映射

使用 `users.external_id` 绑定外部用户 ID。

规则：

- 外部用户首次访问时，如果 `external_id` 不存在，则创建 `users`
- 外部用户再次访问时，通过 `external_id` 找到已有用户
- `auth_source = external`
- 外部用户可以不使用本地密码

## 多实例 WebSocket 投递

### 目标

支持多个服务实例部署时，用户连接在任意实例上都能收到消息。

### 推荐组件

```text
Redis
Redis Pub/Sub
```

### 流程

1. 用户连接到某个服务实例。
2. 服务实例把连接状态写入 Redis。
3. 用户发送消息后，消息先入库。
4. 当前实例向 Redis Pub/Sub 发布投递事件。
5. 所有实例订阅投递事件。
6. 每个实例检查本机是否有目标用户连接。
7. 有连接的实例执行 WebSocket 投递。

### 边界

- Redis 不作为消息最终存储
- MySQL 中的 `messages` 仍然是消息事实来源
- Pub/Sub 丢事件时，客户端仍可以通过历史消息补齐

## 在线状态

### 目标

提供用户在线、离线和最后活跃时间。

### 数据来源

- WebSocket 连接状态
- Redis 在线状态
- `user_devices.last_seen_at`

### 接口建议

```text
GET /api/users/{user_id}/presence
POST /api/users/presence/batch
```

响应示例：

```json
{
  "code": 200,
  "data": {
    "user_id": 2,
    "online": true,
    "last_seen_at": "2026-06-29T12:00:00+08:00"
  },
  "message": "获取在线状态成功"
}
```

规则：

- 有任意活跃 WebSocket 连接时视为在线
- 没有连接时视为离线
- `last_seen_at` 可以来自设备表或 Redis 状态

## 精细已读回执

### 目标

支持查询某条消息谁已读、谁已送达。

### 数据库

启用：

- `message_receipts`

### 接口建议

```text
GET /api/messages/{message_id}/receipts
POST /api/messages/{message_id}/delivered
POST /api/messages/{message_id}/read
```

规则：

- 收到 WebSocket 投递后可以写入 `delivered`
- 用户打开或读到消息时写入 `read`
- `read` 包含 `delivered` 语义
- 会话列表未读数仍可继续使用 `conversation_members.last_read_message_id`

## 消息搜索

### 目标

支持在会话内或全局搜索历史消息。

### 接口建议

```text
GET /api/conversations/{conversation_id}/messages/search?q=hello
GET /api/messages/search?q=hello
```

规则：

- 当前用户只能搜索自己有权限访问的会话
- 搜索结果只返回未删除消息
- 初期可以只支持文本消息搜索
- 数据量变大后建议引入独立搜索索引

## 边界规则

- v7 不要求一次性全部实现，可以拆成身份、投递、回执、搜索多个子版本
- Redis 状态不能替代 MySQL 持久化消息
- 外部身份接入不能破坏本地用户登录
- 精细已读不能影响 v3 的基础未读数计算

## 错误处理

- `400`：参数校验失败
- `401`：登录态无效
- `403`：无权访问目标资源
- `404`：用户、消息或会话不存在
- `500`：服务器内部错误

## 验证方式

1. 外部用户首次访问 IM，确认自动创建或同步 `users`。
2. 外部用户再次访问，确认复用同一个内部用户 ID。
3. 启动两个服务实例，分别建立不同用户连接。
4. 一个实例发送消息，另一个实例上的用户能收到投递。
5. 用户读取消息后，查询消息回执能看到 `read` 状态。
6. 搜索关键词，确认只返回当前用户有权限访问的消息。

## 本版本不包含

- 复杂组织架构同步
- 端到端加密
- 消息分表完整方案
- 搜索引擎部署细节
- 推送服务厂商接入细节
