# 数据库表创建-设备

## 版本归属

用于 v5 设备与离线同步版。

## 表清单

- `user_devices`

## user_devices

用户设备表。用于记录用户设备、在线时间和后续移动端离线推送信息。

## 执行 SQL

```sql
CREATE TABLE IF NOT EXISTS user_devices (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  user_id BIGINT NOT NULL,
  device_id VARCHAR(128) NOT NULL,
  platform ENUM('web', 'ios', 'android', 'desktop') NOT NULL,
  push_token VARCHAR(255) NULL,
  last_seen_at DATETIME NULL,
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY uk_user_devices_user_device (user_id, device_id)
);
```

字段说明：

- `id`：设备记录 ID
- `user_id`：所属用户 ID
- `device_id`：客户端生成或设备侧提供的唯一设备标识
- `platform`：设备平台，支持 `web`、`ios`、`android`、`desktop`
- `push_token`：离线推送 token，当前可以为空
- `last_seen_at`：最近在线或设备上报时间
- `created_at`：创建时间
- `updated_at`：更新时间

索引说明：

- `uk_user_devices_user_device`：保证同一用户同一设备只有一条记录，重复上报时更新已有记录

业务约束：

- 用户只能新增、查询、删除自己的设备记录
- `device_id` 不能为空
- `platform` 必须是允许的枚举值
- v5 只保存 `push_token`，不真正调用 APNs、FCM 或厂商推送
- WebSocket 活跃、设备上报或客户端重新上线时可以更新 `last_seen_at`

## 建表后检查

```sql
DESC user_devices;
SHOW INDEX FROM user_devices;
```

## 最小验证

以下 SQL 假设 `users` 表中已经存在 `id = 1` 的用户。

上报设备：

```sql
INSERT INTO user_devices (user_id, device_id, platform, push_token, last_seen_at)
VALUES (1, 'web-001', 'web', NULL, NOW());
```

重复上报时更新：

```sql
INSERT INTO user_devices (user_id, device_id, platform, push_token, last_seen_at)
VALUES (1, 'web-001', 'web', 'token-001', NOW())
ON DUPLICATE KEY UPDATE
  platform = VALUES(platform),
  push_token = VALUES(push_token),
  last_seen_at = VALUES(last_seen_at);
```

查询用户设备：

```sql
SELECT device_id, platform, push_token, last_seen_at
FROM user_devices
WHERE user_id = 1
ORDER BY last_seen_at DESC;
```

## 代码落地点

- `models/device_model.go` 定义 `UserDevice`
- 上报设备使用 `(user_id, device_id)` 做 upsert
- WebSocket 建连或心跳时可以更新 `last_seen_at`
- 删除设备只允许删除当前登录用户自己的记录
